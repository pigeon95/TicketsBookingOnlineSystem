<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Kinopol", "Repertoire", "Repertoire", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Repertuar", "Repertoire", "Repertoire")</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Dramaty", "Repertoire", "Repertoire")</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Komedie", "Repertoire", "Repertoire")</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Horrory", "Repertoire", "Repertoire")</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Kino akcji", "Repertoire", "Repertoire")</li>
                </ul>
                @Html.Partial("_LoginPartial")
            </div>

        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <br>
        <center>
            <footer>
                <p>&copy; @DateTime.Now.Year - Powered by Gołąb development</p>
            </footer>
        </center>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
